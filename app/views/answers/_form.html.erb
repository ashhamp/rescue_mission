<h1>New Answer for <%= @question.title %></h1>

<%= form_for [@question, @answer] do |f| %>
  <div class="row">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit 'Answer' %>
  </div>
<% end %>
