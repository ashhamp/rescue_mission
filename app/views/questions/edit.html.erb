<h1>Edit Question</h1>

<%= render 'flash' %>

<%= render 'form' %>

<%= link_to "Delete Question", question_path(@question), method: :delete, class: :button %>

<%= link_to 'Back', question_path, class: :button %>
